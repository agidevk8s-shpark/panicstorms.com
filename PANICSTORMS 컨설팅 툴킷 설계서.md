# PANICSTORMS 컨설팅 툴킷 설계서

KO+DA 컨설팅 엔게이지먼트 전 과정(영업 → 진단 → 제안)에서 쓰는 6개 산출물의 기능 명세입니다. 전부 서버 없는 단일 HTML 파일로 제작되어 브라우저에서 바로 열리며, 입력 상태는 `localStorage`에 자동 저장됩니다.

---

## 1. Build Your Legacy — 고객사 소개 피치덱

**목적** 15분 발표 + 15분 Q&A 구조의 KO+DA 클라이언트 소개 프레젠테이션.

**대상 사용자** PANICSTORMS 컨설턴트 (발표자), 잠재 고객사 의사결정권자 (청중).

**구성** 19장 카드형 슬라이드(PPT 스타일) — 발표용 13장(문제정의 → 시장 → 역할정의 → 아키텍처 → 솔루션 트랙 → 도입효과 → 산업별 적용 → 로드맵 → 표준·인증 → CTA) + Q&A 백업 5장(보안, 가격/ROI, 차별점, 리스크, 기술스택).

**핵심 설계 결정**
- Track 01(Flow)/02(Anchor)는 "상용 패키지가 아닌 컨설팅으로 함께 설계하는 영역"임을 슬라이드 내 명시 — 실제 보유하지 않은 제품을 보유한 것처럼 오해시키지 않기 위함.
- 모든 통계는 출처(Gartner, McKinsey, IAPP 2025 등)를 각주로 표기.
- 우측 도트 내비게이션 + 방향키로 슬라이드 이동, 스크롤 스냅.

**출력** 브라우저 뷰(HTML) + 헤드리스 브라우저로 렌더링한 인쇄용 PDF(19페이지, 16:9).

**파일** `Build Your Legacy.html` / `Build Your Legacy.pdf`

---

## 2. 지식자산 진단표 — 첫 미팅용 빠른 진단

**목적** 영업/첫 미팅 자리에서 5분 내로 답할 수 있는 가벼운 진단, 대화의 물꼬를 트는 용도.

**대상 사용자** 고객사 의사결정권자와의 첫 미팅 자리 (실무자 대상 아님).

**핵심 입력값** 문항당 0~3점 (없음/부분적/보통/체계적), 3개 영역(지식 흐름 관리·기록 보존/무결성·AI 거버넌스 준비도) × 각 4~5문항 = 총 14문항.

**로직**
- 영역별 성숙도(%) = (응답 합계 ÷ 응답 문항 수×3) × 100, 응답한 문항만 분모에 포함.
- 상태 밴드: 40% 미만 위험(빨강) / 40~70% 보통(골드) / 70%↑ 양호(초록).
- 우선순위 과제: 응답 중 최저점 3개를 자동 추출, 소속 영역에 따라 Track 01/02 태그 부착.

**화면 구성** 상단 sticky 점수 링(원형 %) + 상태 pill + 영역별 미니 바, 하단 영역 카드 3개, 우선순위 리스트.

**출력** 텍스트 복사, CSV 저장(영역/문항/점수/평가).

**파일** `knowledge-audit-tool.html`

---

## 3. 지식자산 심층진단표 — 현업 실무자용 정밀 진단

**목적** 정식 갭 분석 리포트의 근거 데이터로 쓸 수 있는 실무자 대상 정밀 진단.

**대상 사용자** 고객사 실무 담당자(팀장급 이하) — 개별 작성 또는 인터뷰 중 컨설턴트가 대신 입력.

**진단표와의 차이점**
- 영역이 4개로 확장 (①과 동일한 3개 + **보안·접근 통제** 신규 — 다중 고객사 프로젝트를 동시에 다루는 IT서비스업 특성상 데이터 격리·권한 관리가 별도 축으로 필요하다는 판단).
- 문항 수 26개 (영역당 6~7개) — 4~5개였던 진단표보다 답변 1개가 영역 점수를 흔드는 폭을 20%대에서 15%대로 낮춤.
- 문항마다 **메모 입력란**을 둬서 구체적 근거·사례를 그 자리에서 기록.

**로직** 진단표와 동일한 성숙도 계산식을 4영역으로 확장 적용. 우선순위는 최저점 5개.

**출력** 응답+메모를 정리한 텍스트 복사(리포트 초안용 문단 형식), CSV 저장.

**파일** `knowledge-audit-deep.html`

---

## 4. 지식자산 ROI 계산기

**목적** 부록 B(피치덱)의 단일 계산식을 3개 레버로 확장한 인터랙티브 ROI 계산기. 계산식을 화면에 그대로 노출해 고객사와 함께 실시간 검증 가능.

**대상 사용자** 진단 결과를 근거로 투자 타당성을 설명하는 자리 (실사~제안 단계).

**핵심 입력값**
- 공통 가정: 직원 수, 평균 시간당 인건비, 연간 근무 주수.
- Lever 1 (정보 탐색): 주간 탐색 건수, 현재/개선후 탐색시간(분).
- Lever 2 (퇴사 인수인계): 연간 이직률, 현재/개선후 재현시간(시간).
- Lever 3 (감사 대응): 연간 대응 횟수, 현재/개선후 준비일수, 투입 인원.

**계산식**
```
Lever1 시간 = 직원수 × 주간건수 × (현재-개선)분 × 근무주수 ÷ 60
Lever2 시간 = (직원수 × 이직률%) × (현재-개선)시간
Lever3 시간 = 대응횟수 × (현재-개선)일 × 투입인원 × 8시간
연간 절감액 = Σ(레버별 시간) × 시간당 인건비
회수기간(개월) = 투자비용 ÷ (연간절감액 ÷ 12)
```

**설계 결정** 지식자산 재활용률·AI 응답 정확도 개선 등 계량화가 불확실한 효과는 레버에 포함하지 않고 "정성적 효과"로 하단에 별도 고지 — 과장된 ROI 제시를 방지.

**출력** CSV 저장(가정값·레버별 결과·총합·회수기간 전체).

**파일** `roi-calculator.html`

---

## 5. 14주 엔게이지먼트 일정표

**목적** 피치덱 로드맵 슬라이드(4단계)를 실제 제안서에 첨부 가능한 주차별 일정표로 상세화.

**대상 사용자** 제안서/계약 단계, 프로젝트 착수 미팅.

**구성** Stage 1~4 각각 컬러 코드(파랑/보라/초록/골드)의 간트차트(Week 1~15+) + 주차별 카드 15개(Week 1~14 + 운영단계).

**핵심 입력값** 고객사명, 프로젝트 착수일(date picker) — 착수일 입력 시 각 Week가 실제 캘린더 날짜(MM/DD~MM/DD)로 환산.

**주차별 카드 구성 요소** 활동 내용, 산출물/마일스톤(있는 경우), 담당 주체 태그(PANICSTORMS 단독 / 공동 / 고객사 필수 참여) — RACI 성격의 책임 구분을 명시해 "누가 무엇을 언제까지" 답을 제안서 자체에 담음.

**마일스톤 3개** 갭 분석 리포트(Week 2 종료) · 설계 승인(Week 6 종료) · 파일럿 완료(Week 14 종료).

**출력** 제안서용 텍스트 복사(마크다운형 불릿), CSV 저장.

**파일** `engagement-roadmap.html`

---

## 6. WBS & 산출물 등록부

**목적** 일정표의 활동을 작업분류체계(WBS) 3단계로 분해하고, 전체 정식 산출물을 승인기준까지 명시한 등록부. 제안서의 "실행 계획" 섹션에 직접 첨부 가능한 수준으로 설계.

**대상 사용자** 제안서 작성, 계약 후 프로젝트 킥오프.

**WBS 구조** Stage(1~4) → 작업패키지(1.1, 2.3 등 — 12개) → 세부작업(1.1.1 등 — 30개). 각 세부작업에 소요일(영업일)·담당·연계 산출물 태그 부착. 소요일 합계는 Stage1(10일)+Stage2(20일)+Stage3(40일) = 70영업일 = 14주로 일정표와 정합.

**산출물 등록부** 12건(마일스톤 4건 포함) — ID, 산출물명, 설명, 관련 WBS 번호, 형식(문서/시스템/교육자료 등), 담당, **승인기준**(예: "고객사 검토 후 서면 확인", "복구 테스트 성공 확인", "의사결정권자 서명"). 승인기준을 명시한 이유: 실제 컨설팅 계약에서 산출물 인수 분쟁을 막는 핵심 조항이기 때문.

**출력** WBS·산출물 등록부 각각 별도 CSV 저장.

**파일** `wbs-deliverables.html`

---

## 공통 기술 사항

- **저장/내보내기**: `xlsx`는 이 플랫폼의 다운로드 허용 확장자에 없어 전 도구가 `CSV`로 통일 — 엑셀에서 더블클릭 시 그대로 열림 (UTF-8 BOM 포함해 한글 깨짐 방지).
- **다운로드 경로 이중화**: claude.ai Artifact 뷰어에서는 `downloads` capability로 저장, 로컬 파일로 직접 열었을 때는 표준 Blob 다운로드로 자동 폴백.
- **상태 저장**: 입력값은 도구별 `localStorage` 키로 자동 저장 — 새로고침해도 진행 상황 유지. "초기화" 버튼으로 명시적 리셋.
- **다크모드**: 전 도구 라이트/다크 모드 대응 (시스템 설정 자동 감지 + 강제 지정 시 우선).
- **브랜드 일관성**: panicstorms.com과 동일한 색상 토큰(`#3D5CFF` 블루 축, `#7C3AED` 바이올렛)과 폰트(Noto Sans KR + Inter)를 전 도구에 재사용.
